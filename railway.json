{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo $TEST_VAR > ./google_secrets.json;  uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
